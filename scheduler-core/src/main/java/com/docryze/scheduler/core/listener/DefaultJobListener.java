package com.docryze.scheduler.core.listener;

import com.docryze.scheduler.core.component.JobContext;

public class DefaultJobListener extends AbstractJobListener {

    public DefaultJobListener(JobContext jobContext) {
        super(jobContext);
    }
}
